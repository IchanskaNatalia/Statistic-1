#include<stdio.h>
#define N 10
int input_array(double ar[N]){
    for(int i =0;i<N;i ++){
        print f("input ar[%d]=",i);
       int ch scanf("%if",&ar[i]);
       if(ch!=1) return i;

    }
    return N;
}
